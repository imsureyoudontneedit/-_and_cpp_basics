#include <stdio.h>
#include <stdlib.h>
/*
char *generate(int size){
    char m[size];
    int i;
    for(i=0;i<size;i++)
        m[i]=10;
    return m;
}
*/
int main()
{
    /*
    int *m=generate(100);
    int i;
    for(i=0;i<100;i++)
        printf("%d   ", m[i]);
    /*
    int size = 9999999999999999;
    int i;
    char *m = malloc(size);
    puts("Memory Allocated");
    getch();
    for(i = 0;i < size; i++)
        m[i]=91;
    puts("Memory Used");
    getch();
    free(m);
    puts("Memory Free");
    getch();
    return 0;*/

    return 0;
}
